-define(LOG_COLUMNS,
        [tag,
         pid,
         event_type,
         stage,
         subscribe,
         rules_module,
         event_source,
         event_target,
         message,
         owner,
         character]).

-define(PID_ID_COLUMNS,
        [tag,
         pid,
         id]).
